# FlightPrint Landing Page

Static landing page for FlightPrint, ready to deploy on Vercel.

## Deploy

Push this folder to a Git repository and import it in Vercel. No build command
is required because the site is plain HTML and CSS.

## Local Preview

Open `index.html` in a browser, or serve the folder with any static file server.
# FlightPrintLandingPage
